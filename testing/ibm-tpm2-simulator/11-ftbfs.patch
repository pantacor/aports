--- ./ibmtpm974/src/CryptRand.c.orig
+++ ./ibmtpm974/src/CryptRand.c
@@ -498,7 +498,6 @@
 		)
 {
     DRBG_SEED        tmpBuf;
-    DRBG_SEED        dfResult;
     //
 #if defined USE_DEBUG_RNG && 1
     // If doing debug, use the input data as the initial setting for the RNG state
